---
import { Ear, Eye, Sparkles } from 'lucide-react'

const pillars = [
  {
    icon: Ear,
    title: 'Escuta sem julgamento',
    description:
      'Você pode chegar como você é - com suas contradições, suas dúvidas, seus silêncios. Aqui não tem resposta certa.',
  },
  {
    icon: Eye,
    title: 'Olhar para o todo',
    description:
      'Gênero, raça, sexualidade, corpo, família, trabalho - tudo que compõe quem você e tem espaço na conversa.',
  },
  {
    icon: Sparkles,
    title: 'Cuidado que transforma',
    description:
      'Não apenas acolher o que dói, mas entender de onde vem - para que ser você mesma doa cada vez menos.',
  },
]
---

<section class="relative bg-accent py-24 md:py-32" id="abordagem">
  <div class="mx-auto max-w-5xl px-6">
    <p class="mb-3 text-center font-sans text-sm font-medium uppercase tracking-widest text-primary">
      Abordagem
    </p>
    <h2 class="text-center font-serif text-3xl font-medium text-accent-foreground md:text-4xl lg:text-5xl text-balance">
      Uma escuta que vai além do óbvio
    </h2>
    <p class="mx-auto mt-6 max-w-2xl text-center font-sans text-base leading-relaxed text-accent-foreground/75">
      Com base na Gestalt-terapia e num olhar humanista e feminista, não olho só para o que vocÊ sente - olho para o contexto em que você vive. Suas relações, sua história, o que o mundo espera de você e o que você espera de si mesma.
    </p>

    <div class="mt-16 grid gap-8 md:grid-cols-3">
      {pillars.map((pillar) => {
        const Icon = pillar.icon
        return (
          <div class="group rounded-3xl bg-accent-foreground/5 p-8 transition-all hover:bg-accent-foreground/10">
            <div class="mb-5 flex h-12 w-12 items-center justify-center rounded-2xl bg-primary/20">
              <Icon className="h-6 w-6 text-primary" />
            </div>
            <h3 class="font-serif text-xl font-semibold text-accent-foreground">{pillar.title}</h3>
            <p class="mt-3 font-sans text-sm leading-relaxed text-accent-foreground/70">
              {pillar.description}
            </p>
          </div>
        )
      })}
    </div>
  </div>
</section>
